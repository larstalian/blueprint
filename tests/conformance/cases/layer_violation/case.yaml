id: layer_violation
patterns:
  - policy.layer_rules
expect:
  validate:
    ok: false
    diagnostics:
      - code: ir.policy_layer
        path: .arch/units/payment_service.yaml#consumes.0
        message: unit 'payment_service' in layer 'service' cannot consume contract 'payment_gateway_contract' from unit 'payment_gateway' in layer 'service'
